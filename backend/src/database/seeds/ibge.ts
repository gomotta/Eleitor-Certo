// Seed IBGE não aplicável — banco ELEITOR_CERTO já tem estrutura própria de geo
export async function seedIBGE() {
  console.log('[seed] IBGE: banco já possui estrutura geográfica, seed ignorado.');
}
